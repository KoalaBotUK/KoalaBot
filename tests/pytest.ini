[pytest]
testpaths = tests
python_files = test_*.py
markers =
    first: mark a test as first to be run.
addopts = --ignore=tests/tests_utils/
env =
    CONFIG_PATH=./config-testing
    LOGGING_FILE=False
