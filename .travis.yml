dist: xenial
sudo: required


language: python
python:
  - 3.7
  - 3.8
  - 3.9

install:
  - python --version
  - sudo apt-get install -y libsqlcipher-dev
  - pip3 install -r requirements.txt

script:
  - python -m pytest tests
